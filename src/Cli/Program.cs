namespace PrinterRescue.Cli;

/// <summary>Ponto de entrada da CLI. Implementação completa no Lote B (fachada fina do mesmo motor).</summary>
public static class Program
{
    public static int Main(string[] args) => 32; // ExitCode.UnhandledError até o Lote B
}
